function stringLength(one, two, tree) {

    console.log(one.length + two.length + tree.length);
    console.log(Math.round((one.length + two.length + tree.length) / 3));


}
stringLength('pasta', '5', '22.3')